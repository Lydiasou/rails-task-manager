<%= form_for(task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, placeholder: 'title'%>
  <%= f.label :details %>
  <%= f.text_field :details, placeholder: 'details' %>
  <%= f.label :completed %>
  <%= f.check_box :completed %>
  <%= f.submit%>
<% end %>
